name: mcp-server-azure-functions
metadata:
  template: remote-mcp-functions-typescript
services:
  mcp:
    project: .
    language: ts
    host: function
    hooks:
      prepackage:
        shell: sh
        run: npm run build
